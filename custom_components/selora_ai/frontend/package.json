{
  "name": "selora-ai-panel",
  "private": true,
  "scripts": {
    "build": "esbuild src/panel.js --bundle --format=esm --outfile=panel.js && esbuild src/card.js --bundle --format=esm --outfile=card.js && node postbuild.js && npx prettier --write panel.js card.js"
  },
  "dependencies": {
    "lit": "^2.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "prettier": "^3.8.1"
  }
}
